syntax = "proto3";

package protos;

service ConsensusApi {
    rpc ConsentData (Payload) returns (Dummy);
    rpc GetConsensusStream (Dummy) returns (stream Payload);
}

message Payload {
    bytes payload = 1;
    bytes proof = 2;
}

message Dummy {
    bool success = 1;
}
